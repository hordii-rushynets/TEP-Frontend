export default interface StaticData {
    auth: {
        notifications: {
            registration: string;
            resetPasswordConfirm: string;
            verificationCodeSend: string;
        }
    }
}